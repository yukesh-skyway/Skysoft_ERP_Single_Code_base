/**
 * @shadcn-scheduler/view-week
 *
 * Thin wrapper around the real WeekView from @sushill/shadcn-scheduler.
 * Gets full 7-day horizontal scroll, drag/resize, conflict detection,
 * dependency arrows, markers, availability shading, and now-line for free.
 *
 * Requires a <SchedulerProvider> from @shadcn-scheduler/shell as an ancestor.
 */
export { WeekView, type WeekViewProps } from '@sushill/shadcn-scheduler'
