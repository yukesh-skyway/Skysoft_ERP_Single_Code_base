/**
 * @shadcn-scheduler/view-day
 *
 * Thin wrapper around the real DayView from @sushill/shadcn-scheduler.
 * Gets virtualization, drag/resize, conflict detection, dependency arrows,
 * markers, availability shading, now-line, and everything else for free.
 *
 * Requires a <SchedulerProvider> from @shadcn-scheduler/shell as an ancestor.
 */
export { DayView, type DayViewProps } from '@sushill/shadcn-scheduler'
